<%= form_with(model: faq, local: true) do |form| %>
  <% if faq.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(faq.errors.count, "error") %> prohibited this faq from being saved:</h2>

      <ul>
      <% faq.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :staff %>
    <%= form.text_field :staff, id: :faq_staff %>
  </div>

  <div class="field">
    <%= form.label :question %>
    <%= form.text_area :question, id: :faq_question %>
  </div>

  <div class="field">
    <%= form.label :answer %>
    <%= form.text_area :answer, id: :faq_answer %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
